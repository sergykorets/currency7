<%= content_tag(:div, '', id: 'transactions', data: {
    controller: 'transactions',
    transactions_target: 'transactions',
    actions: @actions,
    admin: @admin,
    count: @count,
    todays_transactions_count: @todays_transactions_count
}) %>